function  = compute_variance_joints(list_value_joints)
    mean_value_joints =mean(list_value_joints);
    value_variance =0;
    for i =1:size(matrixSegment,1)
        vari = vari + sqrt(sum((matrixSegment(i,:)-meanP).^2));
    end
     vari =vari/(size(matrixSegment,1)-1);
end